allow bootanim shell_data_file:dir search;

# call mediaserver domain and transfer references to it
allow bootanim mediaserver:binder { transfer call };

# Themed resources (bootanimation)
allow bootanim theme_data_file:dir search;
allow bootanim theme_data_file:file r_file_perms;
